package materia;

public class InsertionSort {
	public void ordenar(int vet[]) {
		long trocas = 0;
		
		for(int i = 1;i < vet.length - 1;i ++) {
			int key = vet[i];
			int j = i - 1;
			while(j >= 0 && vet[j] > key){
				vet[j+1] = vet[j];
				j--;
			}
			vet[j+1] = key;
		}
	}
}
