package materia;

public class BubbleSort {

	public void ordenar(int[] vet) {
		boolean troca = true;
		int aux;
		
		while(troca) {
			troca = false;
			for(int i = 0;i < vet.length - 1;i ++) {
				if(vet[i] > vet[i+1]) {
					aux = vet[i];
					vet[i] = vet[i+1];
					vet[i+1] = aux;
					troca = true;
				}
			}
		}
		System.out.println("Ordenação do vetor com BubbleSort");
		for(int i = 0; i < vet.length; i++){
			System.out.println(vet[i]);
		}
	}
}
